export interface UserContentProgressDTO {
  profileId: string;
  userId: string;
  contentId: string;
  contentTitle: string;
  categoryId: string;
  categoryName: string;
  completedAt: string; // ISO
};
